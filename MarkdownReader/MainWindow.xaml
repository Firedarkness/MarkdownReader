<Window x:Class="MarkdownReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarkdownReader"
        xmlns:renderer="clr-namespace:MarkdownHtmlRenderer;assembly=MarkdownHtmlRenderer"
        mc:Ignorable="d"
        Title="Markdown Reader" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="MenuStyle" TargetType="Menu">
            <Setter Property="Background" Value="#2B579A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2B579A"/>
            <Setter Property="Padding" Value="8,5"/>
        </Style>
        <!-- 菜单下拉子项样式 -->
        <Style TargetType="MenuItem" x:Key="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="8,5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5F0FF"/>
                    <Setter Property="Foreground" Value="#2B579A"/>
                </Trigger>
                <Trigger Property="Role" Value="TopLevelHeader">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- 状态栏图标按钮样式 -->
        <Style x:Key="StatusIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B679A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- 侧边栏标题按钮样式 -->
        <Style x:Key="HeadingButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5F0FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 菜单栏 -->
        <Menu Grid.Row="0" Style="{StaticResource MenuStyle}">
            <MenuItem Header="文件(_F)" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="打开(_O)" Command="Open" InputGestureText="Ctrl+O">
                    <MenuItem.Icon>
                        <TextBlock Text="📂" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="退出(_X)" Command="Close"/>
            </MenuItem>
            <MenuItem Header="视图(_V)" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="切换侧边栏" Click="ToggleSidebar_Click" InputGestureText="Ctrl+B"/>
                <Separator/>
                <MenuItem Header="滚动到顶部" Click="ScrollToTop_Click"/>
                <MenuItem Header="滚动到底部" Click="ScrollToEnd_Click"/>
            </MenuItem>
            <MenuItem Header="帮助(_H)" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="关于(_A)" Click="AboutMenuItem_Click"/>
            </MenuItem>
        </Menu>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="0"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 侧边栏 - 标题大纲 -->
            <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="8,0,0,8"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,0,1,0">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" ShadowDepth="1" BlurRadius="4" Opacity="0.15"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 侧边栏标题 -->
                    <Border Grid.Row="0" 
                            Background="#2B579A" 
                            CornerRadius="8,0,0,0"
                            Padding="12,10">
                        <TextBlock Text="📑 目录" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="14"/>
                    </Border>
                    
                    <!-- 标题列表 -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Headings}" Margin="0,8,0,8">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource HeadingButtonStyle}"
                                            Tag="{Binding}"
                                            Click="Heading_Click"
                                            Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}, Path=ActualWidth}">
                                        <TextBlock Text="{Binding IndentedText}" 
                                                   TextTrimming="CharacterEllipsis"
                                                   FontSize="13"/>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- 分隔条 -->
            <GridSplitter Grid.Column="1" 
                          Width="5" 
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Background="Transparent"
                          ShowsPreview="True"/>

            <!-- Markdown预览区域 -->
            <Border Grid.Column="2" Background="White" CornerRadius="0,8,8,8">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" ShadowDepth="2" BlurRadius="8" Opacity="0.2"/>
                </Border.Effect>
                <renderer:MarkdownHtmlControl x:Name="MarkdownViewer" 
                                              VerticalAlignment="Stretch"
                                              HorizontalAlignment="Stretch"/>
            </Border>
        </Grid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2" Background="#2B579A" Foreground="White">
            <!-- 目录切换按钮 -->
            <StatusBarItem>
                <Button x:Name="ToggleSidebarButton"
                    Style="{StaticResource StatusIconButtonStyle}"
                    Click="ToggleSidebar_Click"
                    ToolTip="切换目录 (Ctrl+B)"
                    Visibility="Collapsed"
                    Margin="5,0,0,0"
               >
                    <TextBlock Text="◀"/>
                </Button>
            </StatusBarItem>
            <!-- 刷新按钮 -->
            <StatusBarItem>
                <Button x:Name="RefreshButton"
                    Style="{StaticResource StatusIconButtonStyle}"
                    Click="RefreshButton_Click"
                    ToolTip="刷新 (F5)"
                    Visibility="Collapsed"
               >
                    <TextBlock Text="🔄"/>
                </Button>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="就绪"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <!-- 文件名 -->
                    <TextBlock x:Name="FileNameText" Text="" Margin="10,0,0,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>

    <Window.CommandBindings>
        <CommandBinding Command="Open" Executed="OpenCommand_Executed"/>
        <CommandBinding Command="Close" Executed="CloseCommand_Executed"/>
    </Window.CommandBindings>

    <Window.InputBindings>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="Open"/>
    </Window.InputBindings>
</Window>
